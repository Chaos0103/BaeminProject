spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/delivery
    username: sa
    password:
    driver-class-name: org.h2.Driver
#  security:
#    user:
#      name: user
#      password: 1234

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug